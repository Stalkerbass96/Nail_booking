# Deploy env for docker-compose.deploy.yml
# NOTE: DATABASE_URL is injected by compose and points to service `postgres`.

CRON_SECRET=change-me
ADMIN_AUTH_SECRET=change-me-admin-secret
ADMIN_SEED_PASSWORD=dev-only-change-me
AUTO_CANCEL_INTERVAL_MS=300000
